import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'proyect_sem_v2',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
